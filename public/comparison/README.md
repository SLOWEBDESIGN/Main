# Comparison Images

Replace `outdated-site.svg` and `modernized-site.svg` with your own images while
keeping the filenames unchanged. Use matching dimensions for the cleanest
comparison. The included placeholders are both `1600 x 1000`.

PNG, JPG, or WebP replacements are also supported after updating the two image
extensions in `src/components/comparison-slider.tsx`.
